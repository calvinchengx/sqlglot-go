{
 "dialect": "tsql",
 "sql": "CREATE TRIGGER updview ON vw_employees INSTEAD OF UPDATE AS BEGIN UPDATE employees SET salary = inserted.salary FROM inserted WHERE employees.id = inserted.id END",
 "tree": [
  {
   "c": "sqlglot.expressions.ddl.Command"
  },
  {
   "i": 0,
   "k": "this",
   "v": "CREATE"
  },
  {
   "i": 0,
   "k": "expression",
   "v": " TRIGGER updview ON vw_employees INSTEAD OF UPDATE AS BEGIN UPDATE employees SET salary = inserted.salary FROM inserted WHERE employees.id = inserted.id END"
  }
 ]
}