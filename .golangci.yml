version: "2"
linters:
  default: standard
  enable: [errorlint, gosec, misspell, revive, unconvert, unparam, whitespace]
  settings:
    revive:
      rules:
        - name: exported
          disabled: true
