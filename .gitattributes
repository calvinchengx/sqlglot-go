# The expectations and fixtures are compared byte for byte against what the
# Python reference produced. A checkout that rewrote their line endings would
# make the port look wrong on Windows and right everywhere else, which is the
# worst kind of failure to debug. Nothing in this repository wants translation.
* -text
